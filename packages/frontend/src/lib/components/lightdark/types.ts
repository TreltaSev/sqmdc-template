import type { ClassValue, HTMLInputAttributes } from "svelte/elements";

export type tLightdarkProps = HTMLInputAttributes & {
    // Classes:

    // --- Default Classes:
    lightdarkClass?: ClassValue,
};